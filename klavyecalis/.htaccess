# Apache Sunucu Koruma Ayarları
# Bu dosya Apache sunucuda çalışır

# 1. Dizin Listelemeyi Kapat
Options -Indexes

# 2. Dosya Erişimini Kısıtla
<FilesMatch "\.(js|css|json|md)$">
    # Sadece belirli domain'lerden erişime izin ver
    # SetEnvIf Origin "^https?://(www\.)?higcompany\.com$" AccessControlAllowOrigin=$0
    # Header set Access-Control-Allow-Origin %{AccessControlAllowOrigin}e env=AccessControlAllowOrigin
</FilesMatch>

# 3. Hotlinking Engelleme (Dosyaların başka sitelerden kullanılmasını engelle)
RewriteEngine on
RewriteCond %{HTTP_REFERER} !^$
RewriteCond %{HTTP_REFERER} !^https?://(www\.)?higcompany\.com [NC]
RewriteRule \.(js|css|png|jpg|jpeg|gif|svg|wav)$ - [F,L]

# 4. Cache Kontrolü (Güncel versiyonun yüklenmesini sağla)
<IfModule mod_headers.c>
    <FilesMatch "\.(js|css)$">
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires 0
    </FilesMatch>
</IfModule>

# 5. Güvenlik Header'ları
<IfModule mod_headers.c>
    # XSS Koruması
    Header set X-XSS-Protection "1; mode=block"
    
    # Clickjacking Koruması
    Header set X-Frame-Options "SAMEORIGIN"
    
    # MIME Type Sniffing Koruması
    Header set X-Content-Type-Options "nosniff"
    
    # Referrer Policy
    Header set Referrer-Policy "no-referrer-when-downgrade"
</IfModule>

# 6. Belirli Dosyalara Erişimi Engelle
<FilesMatch "^(PROTECTION_GUIDE\.md|README\.md|\.git.*|\.env)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# 7. PHP Execution Engelleme (eğer PHP dosyası yoksa)
<FilesMatch "\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>

# ============================================
# SEO OPTIMIZASYONLARI
# ============================================

# 8. Gzip Compression (Sayfa hızı için)
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# 9. Browser Caching (Performans için)
<IfModule mod_expires.c>
    ExpiresActive On

    # Images
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"

    # Audio
    ExpiresByType audio/wav "access plus 1 year"
    ExpiresByType audio/mpeg "access plus 1 year"
    ExpiresByType audio/mp3 "access plus 1 year"

    # HTML (kısa cache)
    ExpiresByType text/html "access plus 1 hour"
</IfModule>

# 10. UTF-8 Encoding (Türkçe karakter desteği)
AddDefaultCharset UTF-8

# 11. MIME Types
<IfModule mod_mime.c>
    AddType audio/wav .wav
    AddType audio/mpeg .mp3
    AddType application/javascript .js
    AddType text/css .css
</IfModule>
